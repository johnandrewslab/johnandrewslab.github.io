# Andrews Lab website

Public website: https://johnandrewslab.github.io/

This independently maintained lab website is hosted with GitHub Pages.

Edit `index.html` for text and links, and `style.css` for styling. Images and the favicon are stored alongside the page. No build step is required. GitHub Pages publishes the root of the `main` branch.
